<UserControl x:Class="AmericanSlow.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AmericanSlow.Views"
             xmlns:prism="http://prismlibrary.com/"
             mc:Ignorable="d"
             prism:ViewModelLocator.AutoWireViewModel="True"
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Orientation="Vertical" VerticalAlignment="Center" HorizontalAlignment="Center">
        <TextBox Tag="Login" MinWidth="200" Margin="0 0 0 20 " Text="{Binding Login, Mode=TwoWay}"/>
        <TextBox Tag="Password" MinWidth="200" Text="{Binding Password, Mode=TwoWay}"/>
        <StackPanel Orientation="Horizontal" Margin="0 20" HorizontalAlignment="Center">
            <Button Content="Log in" Margin="20 0"/>
            <Button Content="Sign up" Margin="20 0"/>
        </StackPanel>
        <Button Content="Forgot password"/>
    </StackPanel>
</UserControl>
